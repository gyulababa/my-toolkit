# CODEX_FUTURE_PLANS.md — Roadmap (Not Auto-Executed)

This file is NOT executed by Codex automatically.

Codex must only execute tasks listed in CODEX_TASKS.md unless the user explicitly
instructs otherwise.

---

## Future Plans (Candidates)

FP3 — Persist schema migration registry + upgrade hooks
FP4 — Optional alternate persist backends (memory backend for tests)
FP5 — Tag index acceleration + query DSL (if/when needed)
FP6 — CI pipeline (pytest + guardrails + typing)
