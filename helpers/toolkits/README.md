<!-- helpers/toolkits/README.md -->
helpers/toolkits
Purpose

Protocol and integration toolkits that remain frontend-agnostic. These modules provide reusable IO or protocol helpers without coupling to UI adapters.

Belongs here

Device or network protocol helpers (DDP, WLED)

Shared UI toolkits (spec, state, runtime) that are not adapter-specific

Does not belong here

Adapter-specific UI (my_toolkit/toolkit_adapters/adapters)

Service orchestration and domain binding (services)

Public API (flat list)

ddp

wled_http

ui
